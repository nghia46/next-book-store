import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['placehold.co', 'via.assets.so','picsum.photos', 'www.designforwriters.com'],
  },
};

export default nextConfig;
